<div class="span-22 prepend-1 append-1 last">
	<div id="menu">
		<ul class="menu">
			<li class="first"><%= link_to content_tag(:span, "Home"), root_path %></li>
			<% for category in Category.roots %>
				<li>
					<%= link_to content_tag(:span, category.name.capitalize), shop_path(:category => category.id) %>
					<% unless category.children.empty? %>
						<div>
							<ul>
								<% for sub_category in category.children %>
									<li class="parent">
										<%= link_to content_tag(:span, sub_category.name.capitalize), shop_path(:category => sub_category.id) %>
										<% unless sub_category.children.empty? %>
											<div>
												<ul>
													<% for sub_sub_category in sub_category.children %>
														<li><%= link_to content_tag(:span, sub_sub_category.name.capitalize), shop_path(:category => sub_sub_category.id) %></li>
													<% end %>
												</ul>
											</div>
										<% end %>
									</li>
								<% end %>
							</ul>
						</div>
					<% end %>
				</li>
			<% end %>
			<li><%= link_to content_tag(:span, "Manage Products"), products_path %></li>
		</ul>
	</div>
</div>